[tox]
skipsdist = true
basepython = python3
skipinstall = true

[testenv]
sitepackages = False
passenv =
    S2N_LIBCRYPTO \
    S2ND_HOST \
    S2ND_PORT
deps = -rrequirements.txt
commands = pytest --basetemp={envtmpdir} -l tests/ {posargs}
